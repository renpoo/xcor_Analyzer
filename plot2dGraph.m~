function [ outputGraphFileName ] = plot2dGraph( x, y, xLabelStr, yLabelStr, strTitle, figNumber, pname )

figure( figNumber );
plot( x, arraySubstitute_( y, length( x ) ) );
xlabel( xLabelStr );
ylabel( yLabelStr );
title( strTitle );
fname = strcat( y, ' ', saveImageName, '.jpg');
outputGraphFileName = strcat( pname, '/', fname );
saveas( figNumber, strcat( outputGraphFileName ) );
